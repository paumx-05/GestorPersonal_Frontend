# Usar la imagen base de Node.js con la versión especificada
FROM node:22.14.0-alpine

# Instalar la versión específica de npm
RUN npm install -g npm@10.9.2

# Establecer el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copiar los archivos de dependencias primero (para aprovechar la caché de Docker)
COPY package.json package-lock.json* ./

# Instalar las dependencias del proyecto
RUN npm ci

# Copiar todos los archivos del proyecto
COPY . .

# Construir la aplicación Next.js
RUN npm run build

# Configurar el puerto donde correrá la aplicación
ENV PORT=3000

# Exponer el puerto 3000
EXPOSE 3000

# Comando para iniciar la aplicación en producción
# Next.js usa automáticamente la variable de entorno PORT
CMD ["npm", "start"]
